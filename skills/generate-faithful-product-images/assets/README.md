# Optional image state

P0 manifests, slot plans, approved outputs, rejected versions, and QA records are task-specific runtime artifacts. Keep them outside the skill instructions and attach them to the current workflow or project record.
